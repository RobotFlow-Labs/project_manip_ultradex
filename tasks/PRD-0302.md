# Task PRD-0302: Implement Checkpoint Runner

> PRD: PRD-03 | Depends: PRD-0205  
> Files: 2 | Lines: ~160 | Time: ~75min

## What to Build

Implement the checkpoint-backed inference runner that loads the policy, prepares input tensors, and emits split arm and hand actions.

## Files

### Create: `src/anima_manip_ultradex/inference/runner.py`
```python
class UltraDexRunner:
    def predict(self, raw_pc, robot_pc=None):
        return action
```

### Create: `src/anima_manip_ultradex/inference/postprocess.py`
```python
def split_actions(action):
    return {"arms": None, "hands": None}
```

## Verify

```bash
uv run pytest tests/test_inference_runner.py -k runner -v
```

## Done When

- [ ] runner loads checkpoints without training-only state
- [ ] actions are split into `2x6` and `2x12`
- [ ] runner tests pass
