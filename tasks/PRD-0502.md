# Task PRD-0502: Add Docker Assets

> PRD: PRD-05 | Depends: PRD-0501  
> Files: 2 | Lines: ~100 | Time: ~45min

## What to Build

Add Docker assets for API serving with explicit CUDA-aware and local-dev configuration.

## Files

### Create: `docker/Dockerfile.api`
```dockerfile
FROM python:3.11-slim
```

### Create: `docker/docker-compose.api.yml`
```yaml
services:
  manip-ultradex-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
```

## Verify

```bash
docker compose -f docker/docker-compose.api.yml config
```

## Done When

- [ ] docker config renders cleanly
- [ ] checkpoint mount path is explicit
- [ ] compose file validates
