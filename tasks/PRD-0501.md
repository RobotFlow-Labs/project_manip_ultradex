# Task PRD-0501: Implement FastAPI Schemas and App

> PRD: PRD-05 | Depends: PRD-0302  
> Files: 2 | Lines: ~160 | Time: ~75min

## What to Build

Build the FastAPI service and request/response schemas for point-cloud inference.

## Files

### Create: `src/anima_manip_ultradex/api/schemas.py`
```python
from pydantic import BaseModel


class ScenePointCloudRequest(BaseModel):
    points: list[list[float]]
```

### Create: `src/anima_manip_ultradex/api/app.py`
```python
app = FastAPI()
```

## Verify

```bash
uv run pytest tests/test_api_contract.py -k schemas -v
```

## Done When

- [ ] API app starts
- [ ] request schema validates point lists
- [ ] schema tests pass
