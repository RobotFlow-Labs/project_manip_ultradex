# Task PRD-0204: Implement Transformer and Action Queries

> PRD: PRD-02 | Depends: PRD-0203  
> Files: 2 | Lines: ~180 | Time: ~75min

## What to Build

Implement the decoder-only transformer and four learnable action-query tokens that attend unidirectionally to scene tokens.

## Files

### Create: `src/anima_manip_ultradex/policy/action_queries.py`
```python
class ActionQueryBank(nn.Module):
    def forward(self, batch_size: int):
        return queries
```

### Create: `src/anima_manip_ultradex/policy/transformer.py`
```python
class DecoderOnlyTransformer(nn.Module):
    def forward(self, query_tokens, scene_tokens):
        return fused_tokens
```

## Verify

```bash
uv run pytest tests/test_policy_network.py -k transformer -v
```

## Done When

- [ ] four action-query tokens are emitted
- [ ] scene attention is one-way from queries to scene tokens
- [ ] transformer tests pass
