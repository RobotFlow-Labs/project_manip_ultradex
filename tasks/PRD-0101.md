# Task PRD-0101: Rename Scaffold to MANIP-ULTRADEX

> PRD: PRD-01 | Depends: None  
> Files: 7 | Lines: ~120 | Time: ~60min

## What to Build

Replace stale `RAIJIN` naming with `MANIP-ULTRADEX` naming in the package, pyproject metadata, default config, and test imports so the repo structure matches the project brief.

## Files

### Create: `src/anima_manip_ultradex/__init__.py`
```python
"""MANIP-ULTRADEX package."""

from .version import __version__
```

### Create: `src/anima_manip_ultradex/version.py`
```python
__version__ = "0.1.0"
```

### Create: `tests/test_package_import.py`
```python
def test_package_import() -> None:
    import anima_manip_ultradex

    assert anima_manip_ultradex.__version__
```

## Verify

```bash
uv run pytest tests/test_package_import.py -v
```

## Done When

- [ ] `anima_manip_ultradex` imports cleanly
- [ ] stale `anima_raijin` references are removed from active config and metadata
- [ ] package smoke test passes
