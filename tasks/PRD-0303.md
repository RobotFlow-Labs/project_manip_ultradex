# Task PRD-0303: Add CLI and Export Pipeline

> PRD: PRD-03 | Depends: PRD-0302  
> Files: 2 | Lines: ~180 | Time: ~75min

## What to Build

Add a local CLI for offline inference and an export script that emits Torch and ONNX artifacts, with an MLX hook for later porting.

## Files

### Create: `scripts/run_inference.py`
```python
def main() -> None:
    ...
```

### Create: `scripts/export_policy.py`
```python
def main() -> None:
    ...
```

## Verify

```bash
uv run python scripts/run_inference.py --help
uv run python scripts/export_policy.py --help
```

## Done When

- [ ] both CLIs expose argument help
- [ ] export script writes deterministic filenames
- [ ] smoke commands run
