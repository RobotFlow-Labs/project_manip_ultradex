version: "3.8"
services:
  manip-ultradex:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - /Volumes/AIFlowDev/RobotFlowLabs/datasets:/data:ro
    environment:
      - ANIMA_BACKEND=auto
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
